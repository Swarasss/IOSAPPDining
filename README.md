Food Ordering & Delivery App

Platforms: iOS (iPhone/iPad) – Product follows UX design principles and worsk on both an iPhone and iPad in both vertical and horizontal orientation.

Software: Swift, Xcode, SwiftUI

No persistent storage.

Features

API Integration: Weather API and RandomFacts API.

Data Model: Codable entities (Restaurant, MenuItem, CartItem, Order, OrderStatus).

Order Flow & Status: Cart totals; status view with timeline & ETA; manual refresh.

UI/UX: Four screens (Restaurants, Menu, Cart, Status); loading/error states.

Testing: Unit tests (cart math); one UI test (checkout flow).

cart math works properly.
